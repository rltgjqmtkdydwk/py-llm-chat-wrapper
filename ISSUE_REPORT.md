# 이슈 리포트

## 기본 정보

- **발견일**: 2026-09-20
- **발견 경로**: `pytest test_summarizer.py -v` 실행 중 (일관성 테스트 케이스)
- **심각도**: Medium (기능 자체는 정상이나, 안정성/신뢰성에 영향)
- **상태**: 재현 확인 → 원인 분석 → 수정 완료

## 1. 증상

동일 입력에 대해 짧은 시간 안에 연속으로 3번 API를 호출하는 테스트
(`test_consistency_across_repeated_calls`)에서 3번째 호출이 실패함.

```
google.genai.errors.ServerError: 503 UNAVAILABLE.
{'error': {'message': 'This model is currently experiencing high demand.
Spikes in demand are usually temporary. Please try again later.'}}
```

## 2. 재현 절차

1. `GEMINI_API_KEY` 설정 후 `pytest test_summarizer.py -v` 실행
2. 동일한 프롬프트로 `summarize()`를 짧은 간격으로 3회 연속 호출
3. 3회차 호출에서 503 오류 발생 (매번 100% 재현되지는 않음 => 서버 부하 상태에 따라 달라지는 간헐적 이슈가 원인)

## 3. 원인 분석

- HTTP 상태 코드 503은 클라이언트 측 요청 문제가 아니라 **서버(Gemini API) 측의 일시적 과부하**를 의미함
- 원인 후보:
  1. 무료 티어는 짧은 시간 내 연속 요청에 대한 내부 큐잉/스로틀링이 유료 티어보다 민감함
  - 큐잉 : 당장 처리 못 하는 요청을 큐에 보관 / 민감한 이유 - 짧은 시간 동안 요청이 몰릴 때, 내부 큐에 대기시킬 수 있는 요청의 수가 매우 적거나 아예 없음 -> 트래픽 몰리면 즉시 `429 Too Many Requests` 에러 반환
  - 스로틀링 : 서버 과부하를 막기 위해 강제로 제한 / 민감한 이유 - 무료 티어의 분당 요청 제한 엄격함 + 유로 티어 트래픽 우선 처리
  2. 애플리케이션 코드(`app.py`)에 재시도 로직이 없어서, 서버가 잠깐 불안정한 순간에도 바로 실패로 이어짐
- **결론**: 서버 자체의 버그라기보다는, **클라이언트 쪽에서 일시적 오류에 대한 방어 로직이 없었던 것이 근본 원인**

## 4. 재현 시 영향 범위

- 사용자가 짧은 시간에 요약 기능을 여러 번 사용할 경우 실패를 경험할 수 있음
- 재시도 로직 부재로 인해, 서버가 1~2초 뒤에 정상화되더라도 사용자는 그냥 에러를 보게 됨

## 5. 수정 내용

`app.py`의 `summarize()` 함수에 **지수 백오프(exponential backoff) 재시도 로직** 추가:

- 503 등 `ServerError` 발생 시 최대 3회까지 재시도
- 재시도 간격: 1.5초 → 3초 → 6초 (지수적으로 증가시켜 서버 부하를 더 가중시키지 않도록 함)
- 최대 재시도 횟수를 넘기면 원래 예외를 그대로 전파해서, 호출부가 실패 사실을 인지할 수 있도록 함

## 6. 회귀 테스트 추가

- `test_retries_on_server_error_then_succeeds`: 1번 실패 후 재시도로 성공하는 케이스를 mock으로 검증
- `test_gives_up_after_max_retries`: 계속 실패하면 결국 예외가 전파되는지 검증
- 실제 API를 호출하지 않고 mock으로 검증하므로, 외부 서버 상태와 무관하게 항상 안정적으로 실행 가능

## 7. 배운 점

- LLM API는 일반 REST API보다 **일시적 가용성 문제(특히 무료 티어)** 가 잦아서, 재시도/백오프 전략이 실무에서는 필수에 가깝다는 걸 체감함
- "버그처럼 보이는 실패"가 실제로는 "코드의 방어 로직 부재"인 경우가 있다는 걸 직접 꺠달음
  — 원인을 서버 탓으로 돌리지 않고, 내 코드가 이 상황에 어떻게 대응해야 하는지를 먼저 점검해야함
- 재현이 100%가 아닌(간헐적인) 이슈를 다룰 때는, 재현 절차를 "몇 번 시도 중 몇 번 발생하는지" 식으로 기록해두는 게 나중에 원인 분석에 도움이 됨
